/* My comments */

#import "world.map"

    b as boolean;
    i as integer;
    s as boolean[2, 5];

main as function():void
do

   i := 0 + 42 * 1;
   i := 42 + (3 + i);
   i := (-34 - 2) * (5 - i);
   i := (i % 2) + 5;

dig();

end