#import "world.map"

x as integer;
y as integer;

main as function(): void
do
    x := 1;
    y := 2;

    if (x < y) then
        jump();
        dig();
    else
        left();
        right();
    end

    dig();
end