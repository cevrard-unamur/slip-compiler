/* My comments */

#import "world.map"

    b, k, m as boolean;
    i as integer[42];
    i2 as integer[42, 42];
    s as boolean[2, 5];

main as function():void
do

   b := true and false or true;
   b := not(false && (0 > 45 + 3));
   s[0, 1] := b and false;
   s[0, 2] := not b;

    dig();

end