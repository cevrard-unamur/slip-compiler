/* Test If playPlus */

#import "world.map"


    i as integer;
    x as integer;
    b as boolean;
    s as char;
    t1 as boolean[5];
    t2 as integer[6];
    t3 as char[3];


main as function():void
do
   i := 2;
   b := true;
   b := (true and (not b));
   x := 3;

        if (b) then
           left(x);
        end

        if ((b or true) and false) then
        	 left(i);
        end

        t1[0] := b;
        t1[1] := false;
        t1[2] := b;
        t1[3] := (not (b and true));
        t1[4] := t1[0];



        if (t1[0] and t1[1]) then

           b := true;
           fight();
        end

        t2[0] := 2;
        t2[1] := 1;
        t2[2] := 10;
        t2[3] := 3;
        t2[4] := 1;
        t2[5] := 3;

        if (not b) then

        	t2[0] := t2[1] + t2[2];
        	up();
        end

        if (i <> 0) then
        	b := false;
        	down(5);
        end

        if (t2[i + 1] > 2) then
        	i := (i - 67) + (i - 45);
        	fight();
            dig();
            up(3);
            left(2);
        end

dig();

end